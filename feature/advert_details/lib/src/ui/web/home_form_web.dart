import 'package:flutter/material.dart';

class HomeFormWeb extends StatelessWidget {
  const HomeFormWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      color: Colors.green,
    );
  }
}
